# Core Supervisor / Engine runtime dependencies.
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
httpx>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
PyYAML>=6.0.1
mcp>=1.9.0,<2

# Web first-deployment setup and login support.
bcrypt>=4.1.0
PyJWT>=2.8.0

# Discord adapter (adapters/discord/).
discord.py>=2.6.0
aiohttp>=3.14.0
brotlicffi>=1.1.0          # aiohttp C parser needs this for Brotli (br) decoding

# Optional platform adapters are documented separately:
# - Discord: adapters/discord/README.md
# - OneBot 11 / QQ: adapters/onebot/README.md
# Web frontend dependencies are managed by platform/web/frontend/package-lock.json.
